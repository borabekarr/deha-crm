# src/types/

Global TypeScript types and shared interfaces used across the entire application (e.g., `User`, `Supabase` database types, API response shapes).
Types scoped to a single feature belong in that feature's folder, not here.
Do NOT put runtime code, components, or hooks in this directory — types and interfaces only.
