# PRUNE-PROPOSAL — component inventory sweep

**Date:** 2026-07-03 · **Author:** Step 11 executor (dept-audit sweep) · **Status:** PROPOSE-ONLY, nothing deleted.

## Terminology contract (verify-before-claim)

The action proposed here is **UNLINK**, never delete. Source files in
`~/claude-code-system/.claude/` stay alive on disk. An unlinked component loses its
**registration**: its entry is removed from `.claude/dept-ownership.json` (owning dept
array), the owning `agents/dept-*.resources.json` catalog is regenerated by
`scripts/build-dept-manifest.sh`, and the metadata index is rebuilt by
`hooks/metadata-build.cjs build`. The SKILL.md/reference file itself remains for later
re-promotion via `/promote`. Every card below states the ownership edit that keeps
`scripts/verify-dept-coverage.sh` at **0 orphans / 0 duplicates**.

## Method (mechanical screen, run this session)

Inventory sources read: base index `/home/bora/claude-code-system/.claude/metadata-index.json`
(314 owned), ext overlay `.claude-ext/metadata-index.json` (335), the 8
`agents/dept-*.resources.json` catalogs, `dept-ownership.json` (333 owned entries, 8 depts).

Screens applied:
1. **Base∩ext = 301** shared type:id — confirms ext is a symlinked consumer overlay, NOT
   duplication to cut. Base-only (13) are system-repo memory files; ext-only (34) are
   deha-crm-specific components. No cut signal from the overlay.
2. **Catalog coverage: 0 base components missing from any dept catalog** — every component
   is intent-discoverable; there are no unreachable/orphaned assets. So "dead" here means
   *catalog-listed but never routed-to in prose and never invoked*, not "uncataloged".
3. **Reverse-reference-in-prose** (grep across `agents commands skills references hooks
   scripts lessons CLAUDE.md`, excluding the component's own dir and the auto-generated
   `*.resources.json` catalogs): only two skills scored **zero** — `tech-debt-audit`,
   `tailwind-token-consolidation`.
4. **Trigger/intent overlap** with a stronger sibling: `six-hats`, `landing-page`.
5. **ctx assets = KEEP** (user will re-enable ctx). Screened and excluded from cuts:
   `hook:ctx-scope-guard`, `hook:ctx-sync-on-change`, `hook:ctx-timeout-guard`,
   `reference:ctx-usage-policy`, `lesson:ctx-timeout-discipline`,
   `script:ctx-bootstrap-index`, `agent:semantic-search` (ctx_search tool). NOT proposed.
6. **LOCKED decisions** honored: agentation, agentation-self-driving, platform-design-ios,
   web-design-reviewer, prompt-injection, sql-injection-testing, stitch-builder, coss, all
   review-* subagents = KEEP (referenced directly, no card). gsap = UNLINK (locked).

**Invocation ground-truth (failure-guard against hallucinated kill-list):** grepped all
deha-crm transcripts at `/home/bora/.claude/projects/-home-bora-deha-crm/` for Skill-tool
calls. Command: `grep -rhoF '"name":"<skill>"' <dir> | wc -l`.
Result: `tech-debt-audit=0`, `tailwind-token-consolidation=0`, `six-hats=0`,
`landing-page=0` real Skill invocations. (High raw string counts for these names are
roster-injection noise — the session skill-list, which contains each skill's own trigger
text; none are actual invocations.)

---

# TIER: LOCKED (no card required — decided upstream)

## L1 · skill:gsap — UNLINK (locked)

- **Owner:** dept-frontend. **Ownership edit:** remove `"skill:gsap"` from
  `departments["dept-frontend"]` in `dept-ownership.json`, regenerate frontend manifest.
- **Reverse-ref grep:** `grep -rlF gsap agents commands skills references hooks scripts lessons CLAUDE.md | grep -v skills/gsap/` → **5 hits**: `dept-frontend.resources.json`
  (catalog), `skills/verify-before-claim/SKILL.md`, `references/frontend/animation-vocabulary.md`,
  `references/workflow/premerge-reference-sweep.md`, `lessons/hallucinated-kill-list-2026-05-24.md`.
  All 5 are *meta* mentions (gsap is the canonical worked example of a locked-unlink target),
  not live routing. Sweep those 5 when unlinking. Body: 369 lines, single SKILL.md.
- Confirmed locked per task brief; listed for completeness, not re-litigated.

---

# TIER: HIGH confidence

## H1 · skill:tech-debt-audit — UNLINK (fold into /audit + /deslop + dead-code-audit)

1. **Frontmatter description (verbatim):** "Thorough, user-invoked tech debt and
   architecture audit of the current codebase. Produces TECH_DEBT_AUDIT.md with file-cited
   findings, severity, effort estimates, and a required \"looks bad but is actually fine\"
   section. Use when the user asks for a debt audit, codebase health check, architecture
   review, or code quality assessment of an entire repo. Does not auto-invoke."
   (`disable-model-invocation: true`.)
2. **Triggers:** `["tech debt audit", "codebase health check", "architecture review", "code quality assessment"]`.
3. **Body skim:** 101 lines, single `SKILL.md`. Observed behavior: Phase 1 "Orient" runs
   `git log --oneline -200` + `git log --stat --since="6 months ago"`, lists top-20 largest
   and top-20 churned files, publishes a TodoWrite plan, then audits across dimensions into
   `TECH_DEBT_AUDIT.md`. Generic (language-agnostic) imported prompt; nothing Deha-specific.
4. **Reverse-reference scan (this session):**
   `grep -rlF tech-debt-audit agents commands skills references hooks scripts lessons CLAUDE.md | grep -v skills/tech-debt-audit/`
   → **1 hit**: `agents/dept-testing-qa.resources.json` (auto-generated catalog only).
   `grep -rn tech-debt-audit agents/dept-*.md` → **0** (no dept prose routes to it).
   Excluding the catalog: **0 prose references anywhere.**
5. **git log:** `git log -1 --format='%ci %h %s' -- skills/tech-debt-audit/` →
   `2026-06-14 12:21:39 +0300 0c13446 feat(system): harvest prompts.chat batch + Supabase
   multi-tenant RLS checklist (#20)`. Prior touch `d03ec48 ... archived legacy assets`.
   Last-touched via a **bulk harvest** commit, never edited since.
6. **Transcript/memory scan:** `grep -rhoF '"name":"tech-debt-audit"' /home/bora/.claude/projects/-home-bora-deha-crm/ | wc -l` → **0** real Skill invocations.
   `grep -rlF tech-debt-audit memory` → **0 files**. Never invoked, never noted in memory.
7. **Dependency map:** frontmatter `related: ["skills/audit"]`. No file imports/sources it;
   no hook or script calls it. It points OUT to /audit; nothing points IN.
8. **Sibling files:** `skills/tech-debt-audit/SKILL.md` only — single-file skill, not a
   compound unit. Unlinking removes nothing shared.
9. **Future-phase relevance:** db N (no schema role) · backend N · AI N · web N (repo-wide
   prose audit, not UI) · iOS N · security N (defers to security dept) · ops UNKNOWN→low
   (churn analysis could aid ops, but /audit + `references/system/dead-code-audit.md` already
   cover it) · QA **partial** — this is dept-testing-qa's turf, but the dept already owns
   `command:deslop`, `skill:audit` (KCPM pruning), `skill:diagnose`, and
   `reference:dead-code-audit` (3-phase methodology). No capability is lost.
10. **Counter-example hunt:** searched for any workflow that *requires* a TECH_DEBT_AUDIT.md
    artifact: `grep -rlF TECH_DEBT_AUDIT agents commands hooks scripts references` → **0
    hits** outside the skill itself. No consumer depends on its output.
11. **Owner-context:** author `Bora`, but via the `#20` "harvest prompts.chat batch" bulk
    import — an ingestion commit, not a deliberate keep. No memory note, no lesson, no prior
    "keep tech-debt-audit" mention (`grep -rlF tech-debt-audit memory lessons` → 0). No owner
    keep-signal to override the claim.

- **Ownership edit:** remove `"skill:tech-debt-audit"` from
  `departments["dept-testing-qa"]` in `dept-ownership.json`; regenerate
  `dept-testing-qa.resources.json`; rebuild metadata. verify-dept-coverage stays 0/0.

**Claim:** unlink (capability fully covered by /audit + /deslop + dead-code-audit reference).
**Confidence:** high. **Blocker:** none.

## H2 · skill:tailwind-token-consolidation — UNLINK (niche, zero use, fold under design-system)

1. **Frontmatter description (verbatim):** "Reduce, dedupe, or consolidate CSS custom
   properties and Tailwind v4 design tokens in globals.css. Triggers on: 'reduce tokens',
   'too many tokens', 'dedupe globals.css', 'shrink @theme inline', 'collapse similar
   colors', 'standardize design tokens', 'dead tokens', or any task targeting fewer tokens
   with preserved visuals. Covers @theme inline, --color-* aliases, :root/.dark blocks,
   dead-token analysis, value-similar collapse." (header comment:
   `adapted from evaluate/millionco-skills`.)
2. **Triggers:** `["reduce tokens", "too many tokens", "dedupe globals.css", "shrink @theme inline", "collapse similar colors", "standardize design tokens", "dead tokens", "token consolidation"]`.
3. **Body skim:** 110 lines, single `SKILL.md`. Observed behavior: builds a transitive LIVE
   token set (utility-class suffix OR `var(--name)` OR referenced inside another live
   token), emits a deterministic `old→new|DELETE` rename map applied by one script across
   globals.css + all sources, verifies with typecheck/lint/build. Assumes a Tailwind-v4
   `globals.css` + `@theme inline`.
4. **Reverse-reference scan (this session):**
   `grep -rlF tailwind-token-consolidation agents commands skills references hooks scripts lessons CLAUDE.md | grep -v skills/tailwind-token-consolidation/`
   → **1 hit**: `agents/dept-frontend.resources.json` (catalog only).
   `grep -rn tailwind-token-consolidation agents/dept-*.md` → **0**. Excluding catalog: **0
   prose references.**
5. **git log:** `git log -1 --format='%ci %h %s' -- skills/tailwind-token-consolidation/` →
   `2026-06-28 14:49:21 +0000 2811ab4 feat(system): scoped-ctx re-enable, memory discipline
   + audit, eval promotions`. Added ~5 days ago as an eval promotion; untouched since.
6. **Transcript/memory scan:** `grep -rhoF '"name":"tailwind-token-consolidation"' <deha-crm transcripts> | wc -l` → **0** real Skill invocations.
   `grep -rlF tailwind-token-consolidation memory` → **1 file**: `memory/evaluated_resources.md`
   (the IRS eval record that admitted it — provenance, not usage).
7. **Dependency map:** frontmatter `related: ["skills/baseline-ui/SKILL.md",
   "skills/web-performance-optimization/SKILL.md"]`. No importer; points OUT only.
8. **Sibling files:** `skills/tailwind-token-consolidation/SKILL.md` only. Single-file, not
   a unit.
9. **Future-phase relevance:** db N · backend N · AI N · web **partial** — token hygiene is
   real frontend work, BUT the Deha design system is governed by
   `references/frontend/design-system.md` + design-system-extended + the DS-lock hooks, and
   the frontend dept already owns `command:design-system` (token generation/audit) and
   `skill:baseline-ui`. This skill is a narrow one-file-refactor niche (millionco's
   globals.css shrink) with no observed trigger in a CRM whose CSS lives in per-component
   `.css`, not a monolithic globals.css. iOS N · security N · ops N · QA N.
10. **Counter-example hunt:** searched for any project globals.css this skill would target:
    `grep -rl '@theme inline' /home/bora/deha-crm/apps` → **checked, the DS uses per-component
    CSS + design tokens, no single oversized globals.css@theme requiring dedup**. No live
    surface demands this skill.
11. **Owner-context:** admitted via `/evaluate` (IRS) and promoted in `2811ab4`. The eval
    record is the only owner mention; no follow-up use, no "keep" note beyond the initial
    admission. Recency (5 days) is the one caution — see Blocker.

- **Ownership edit:** remove `"skill:tailwind-token-consolidation"` from
  `departments["dept-frontend"]`; regenerate `dept-frontend.resources.json`; rebuild metadata.

**Claim:** unlink (narrow imported niche, 0 invocations, covered by design-system dept).
**Confidence:** high on redundancy, tempered to **medium-high** by 5-day recency.
**Blocker:** none hard; flag for user — it was *just* promoted, so confirm it wasn't admitted
for an imminent token-cleanup task before unlinking.

---

# TIER: MEDIUM confidence

## M1 · skill:six-hats — UNLINK (decision-framework overlap with ultra-think + premortem)

1. **Frontmatter description (verbatim):** "Structured six-hats thinking sessions for
   debating decisions. Use when user says 'six hats', 'six hats debate', or wants to explore
   a decision through six colored-perspective lenses in sequential rounds with a final
   recommendation." (header: `adapted from juanallo/six-hats-skill (whole-file)`.)
2. **Triggers:** `["six hats", "six hats debate", "six hat"]` — narrow, name-only triggers
   (fires only when the user literally says "six hats").
3. **Body skim:** 205 lines, single `SKILL.md`. Observed behavior: 3 sequential rounds over
   White/Red/Yellow/Black/Green/Blue hats, Blue moderates and writes
   `{output_path}/debate-{timestamp}.md` with a final recommendation. Whole-file upstream
   import, verbatim de Bono method.
4. **Reverse-reference scan (this session):**
   `grep -rlF six-hats agents commands skills references hooks scripts lessons CLAUDE.md | grep -v skills/six-hats/`
   → **2 hits**: `agents/dept-planning-workflow.resources.json` (catalog),
   `skills/premortem/SKILL.md` (names six-hats as a *sibling alternative* framework).
   `grep -rn six-hats agents/dept-*.md` → **0** dept-body routing.
5. **git log:** `git log -1 --format='%ci %h %s' -- skills/six-hats/` →
   `2026-05-10 16:26:20 +0000 d03ec48 feat: system overhaul — new commands, archived legacy
   assets, hook updates`. **Oldest of the candidates (~8 weeks), untouched since May.**
6. **Transcript/memory scan:** real Skill invocations **0**. `grep -rlF six-hats memory` →
   **0 files**. Never invoked, never noted.
7. **Dependency map:** frontmatter `related: ["commands/ultra-think", "commands/planning"]`.
   Referenced-in by premortem only, as an alternative — soft, not a dependency.
8. **Sibling files:** `skills/six-hats/SKILL.md` only. Single-file, not a unit.
9. **Future-phase relevance:** db N · backend N · AI N · web N · iOS N · security N · ops
   **low** · QA N. This is a general decision-debate tool. The planning-workflow dept
   already owns `command:ultra-think` (adversarial multi-framework analysis) and
   `skill:premortem` (failure-backcast) — two stronger, better-integrated decision lenses.
   Six-hats adds a third overlapping frame with the weakest trigger surface.
10. **Counter-example hunt:** is six-hats ever *required* by a plan/skill? searched
    `grep -rlF six-hats references plans` (system) → **0**. premortem merely lists it; nothing
    depends on it.
11. **Owner-context:** author `Bora` via the May system-overhaul bulk commit; a whole-file
    upstream import. No memory/lesson keep-signal (`grep -rlF six-hats memory lessons` → 0).

- **Ownership edit:** remove `"skill:six-hats"` from
  `departments["dept-planning-workflow"]`; regenerate that manifest; rebuild metadata.

**Claim:** unlink (redundant decision framework; ultra-think + premortem cover the intent).
**Confidence:** medium — it's cheap to keep and genuinely distinct in method; the case is
redundancy + zero use, not deadness. **Blocker:** none; low-stakes, user's call.

## M2 · skill:landing-page — UNLINK candidate, BLOCKED on future-phase UNKNOWN

1. **Frontmatter description (verbatim):** "Use when designing, writing, or rewriting a
   high-converting landing page (single-offer page) for SaaS/apps/services. Triggers on:
   '/landing-page', 'design a landing page', 'marketing page', 'hero section', 'rewrite
   landing page', 'high-converting page'. Covers intake questions, section structure,
   conversion strategies, copywriting templates, and a structured output format."
2. **Triggers:** `["/landing-page", "design a landing page", "marketing page", "hero section", "landing page", "design landing page", "rewrite landing page", "high-converting page"]`.
3. **Body skim:** 207 lines, single `SKILL.md`. Observed behavior: mandates reading
   component-library-registry + design-system + anti-slop-ui, then runs an intake
   (one primary action / offer / ICP), section structure, and copywriting templates. Well
   authored, Deha-design-system-aware (loads local references).
4. **Reverse-reference scan (this session):**
   `grep -rlF landing-page agents commands skills references hooks scripts lessons CLAUDE.md | grep -v skills/landing-page/`
   → **3 hits**: `agents/dept-frontend.resources.json` (catalog), `commands/ux-import.md`
   (mentions landing pages as a pattern context), `references/frontend/animation-discipline.md`
   (uses "landing page" as an example phrase). `grep -rn landing-page agents/dept-*.md` → **0**
   dept-body routing.
5. **git log:** `git log -1 ... -- skills/landing-page/` →
   `2026-06-14 12:21:39 +0300 0c13446 feat(system): harvest prompts.chat batch ... (#20)`.
   Same bulk harvest as tech-debt-audit; untouched since.
6. **Transcript/memory scan:** real Skill invocations **0**. `grep -rlF landing-page memory`
   → **0 files**.
7. **Dependency map:** frontmatter `related: ["commands/frontend-design", "skills/baseline-ui"]`.
   Points OUT to the greenfield-page command; ux-import/animation-discipline mention it in
   prose but do not depend on it.
8. **Sibling files:** `skills/landing-page/SKILL.md` only. Single-file, not a unit.
9. **Future-phase relevance:** db N · backend N · AI N · **web UNKNOWN** — Deha CRM is an
   internal app with no public marketing surface *today*, but the repo has a
   `reference:crm-sales-enablement` (ext) and a landing/marketing page is a plausible near
   phase for a commercial CRM. iOS N · security N · ops N · QA N. **This UNKNOWN on the web
   axis is a hard blocker per the verify-before-claim rule (item 9 UNKNOWN = blocker).**
10. **Counter-example hunt:** any plan or roadmap that anticipates a marketing page?
    `grep -rlF 'landing page' /home/bora/deha-crm/plans /home/bora/deha-crm/memory` → checked,
    **0 direct roadmap hits**, but absence of a plan does not prove absence of intent.
11. **Owner-context:** bulk-harvest import (#20). No keep-signal, but also no
    disprove-signal — the future-phase question is open.

- **Ownership edit (if approved):** remove `"skill:landing-page"` from
  `departments["dept-frontend"]`; regenerate manifest; rebuild metadata.

**Claim:** unlink — **but held back**. **Confidence:** low. **Blocker:** future-phase web
relevance is UNKNOWN (possible commercial-CRM marketing page). Do NOT unlink until the user
confirms no landing/marketing surface is on the roadmap. Presented for an explicit user call.

---

# CONSIDERED → KEEP (screened, no cut)

These surfaced in the mechanical screen but the evidence says keep. Listed so the review is
complete and reproducible.

- **skill:style-transfer** — KEEP. Reverse-ref grep returned **5 hits including live routing**:
  `agents/dept-frontend.md` (dept BODY prose), `commands/frontend-design.md`,
  `skills/baseline-ui/SKILL.md`. It is wired into the frontend build workflow, not
  catalog-only. Not a cut.
- **skill:rsvp** — KEEP. User's own Turkish speed-read TUI (`git log` shows it was
  hand-promoted from ext in `#14`/`#15`). `grep -rlF rsvp` → catalog +
  `lessons/delegate-visual-debug-to-subagents.md`. Personal, deliberate, zero-token local
  tool; owner keep-signal is implicit in the promotion history. Not a cut.
- **reference:dead-code-detection (frontend) vs reference:dead-code-audit (system)** — KEEP
  BOTH. Read both heads this session: `dead-code-detection` = cross-file *static detectors*
  with confidence tiers (mined from millionco/deslop-js); `dead-code-audit` = a 3-phase
  *methodology* (Discovery/Verification/Triage) for full sweeps. Distinct scope (detectors
  vs process), owned by different depts (frontend vs testing-qa). Not a duplicate; a
  low-confidence future merge at most, not proposed now.
- **ctx assets** (ctx-scope-guard, ctx-sync-on-change, ctx-timeout-guard, ctx-usage-policy,
  ctx-timeout-discipline, ctx-bootstrap-index, semantic-search) — KEEP per brief; ctx is
  disabled but the user will re-enable it. Not dead.
- **The 8 references that scored 1 catalog-only hit** (animation-vocabulary,
  dead-code-detection, empower-claude-contract, form-validation, react-scan-setup,
  tailwind-v4-patterns, ui-micro-tips, ux-real-world) — KEEP. Catalog-only is the *normal*
  resting state for a leaf reference in this intent-routed system (every dept agent loads
  its `*.resources.json` and shortlists by meaning). Zero prose-refs alone is not a cut
  signal for references; it is for skills only when paired with 0 invocations + a stronger
  sibling (H1/H2). No card opened.

---

# Summary table

| Tier | id | type | owner dept | claim | confidence | blocker |
|---|---|---|---|---|---|---|
| LOCKED | gsap | skill | dept-frontend | unlink | (locked) | none |
| HIGH | tech-debt-audit | skill | dept-testing-qa | unlink | high | none |
| HIGH | tailwind-token-consolidation | skill | dept-frontend | unlink | med-high | 5-day recency, confirm |
| MEDIUM | six-hats | skill | dept-planning-workflow | unlink | medium | none |
| MEDIUM | landing-page | skill | dept-frontend | unlink | low | future-phase UNKNOWN |

**Coverage-safety:** every proposed unlink lists its `dept-ownership.json` edit. After each
edit, run `scripts/build-dept-manifest.sh` + `hooks/metadata-build.cjs build` +
`scripts/verify-dept-coverage.sh` — the gate stays 0 orphans / 0 duplicates because the id
leaves both the base index and the ownership map in the same pass.

**Files changed this step:** this file only. `git status` shows no deletions under any
`.claude` tree. Nothing was unlinked; all items await per-item user approval.
