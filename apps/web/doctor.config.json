{
  "$schema": "https://react.doctor/schema.json",
  "$comment": "no-tiny-text override for design-system showcase: 10px Micro Label is a documented, sanctioned step of the type scale (see TypeScale.tsx), not an accessibility bug. Suppression approved by Bora 2026-07-12. Scoped to this directory only via ignore.overrides so genuinely-too-small text elsewhere still gets flagged.",
  "ignore": {
    "overrides": [
      {
        "files": ["src/components/design-system/**"],
        "rules": ["react-doctor/no-tiny-text"]
      }
    ]
  }
}
