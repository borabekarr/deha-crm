/**
 * NATIVE STUB — expo-apple-maps-sheet
 *
 * Source: https://github.com/rit3zh/expo-apple-maps-sheet
 * Author: rit3zh
 * Classification: native-stub (depends on react-native-maps, expo-glass-effect,
 *   @lodev09/react-native-true-sheet — all require native linking)
 *
 * This stub satisfies TypeScript imports. Runtime behavior requires an
 * EAS dev-client build with native modules linked (Step 11a).
 */
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export interface AppleMapsSheetProps {
  children?: React.ReactNode;
}

export const AppleMapsSheet: React.FC<AppleMapsSheetProps> = () => {
  throw new Error(
    'expo-apple-maps-sheet: vendored from rit3zh; native build pending — install via local path in EAS dev-client build (Step 11a).',
  );
};

export default AppleMapsSheet;

const styles = StyleSheet.create({
  stub: { flex: 1, alignItems: 'center', justifyContent: 'center' },
});
