/**
 * NATIVE STUB — expo-progressive-blur
 *
 * Source: https://github.com/rit3zh/expo-progressive-blur
 * Author: rit3zh
 * Classification: native-stub (example app only; depends on
 *   @react-native-masked-view/masked-view + expo-linear-gradient +
 *   react-native-easing-gradient; no reusable exported library)
 *
 * This stub satisfies TypeScript imports. A proper implementation should
 * wire BlurView + MaskedView + LinearGradient — deferred to Step 11
 * where the EAS dev-client build is available.
 *
 * Runtime usage throws with a clear message.
 */
import React from 'react';

export interface ProgressiveBlurProps {
  children?: React.ReactNode;
  intensity?: number;
  style?: object;
}

export const ProgressiveBlur: React.FC<ProgressiveBlurProps> = () => {
  throw new Error(
    'expo-progressive-blur: vendored from rit3zh; native build pending — install via local path in EAS dev-client build (Step 11a).',
  );
};

export default ProgressiveBlur;
