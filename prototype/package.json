{
  "name": "deha-crm-prototype",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 5500 --bind 127.0.0.1",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
